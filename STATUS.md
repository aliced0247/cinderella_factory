# STATUS — シンデレラ・ファクトリー

## Phase 1：コア生産ループの試作 — ✅ 完成（検品待ち）

最終更新：2026-06-11（codeくん）

### 動かし方

- `index.html` をブラウザで開くだけ（Phaser同梱・通信不要・サーバー不要）
- スマホ実機なら：PCで `npx http-server -p 8080` → スマホで `http://<PCのIP>:8080` を開く（同じWi-Fi）
- GitHub Pagesに置いてもそのまま動く構成

### できていること

- [x] 工房1部屋（20×15マス）。クリーム×シュガーピンクの市松床
- [x] **コア生産ループ1本**：魔法の泉が原石を排出 → ベルトで搬送 → 研磨機で加工（3秒）→ 納品箱で売上化
  - 仮価格：宝石原石=1リル／研磨宝石=5リル（加工した方が儲かる体感◎）
- [x] 設置4種：魔法の泉（2×2・上限2）／ベルト（1×1・4方向・曲がり対応）／研磨機（2×2・入出力口の向きあり）／納品箱（2×2）
- [x] タッチ操作：
  - 下部パレットで選択 → マスをタップで設置（「むき」ボタンで設置向きを回転）
  - **ベルトはドラッグで連続敷設**（なぞった方向に自動で向きが付く。既存ベルトをなぞると引き直し）
  - 設置済みタップ → 「⟳回転／✕撤去」ミニメニュー
  - ピンチズーム＋ドラッグスクロール
- [x] 姫キャラ（32×48）：左下の仮想スティックで歩行。機械はすり抜け不可、ベルトの上は歩ける
- [x] UI：上部に所持リル（金縁 #F2B73F）／下部に建設パレット
- [x] セーブ：配置と所持リルをlocalStorageへ自動保存・起動時復元（変更後1秒デバウンス＋15秒ごと＋タブ非表示時）
- [x] 演出：納品時キラキラ＋「+5リル」フロート、加工中の研磨機からキラキラ粒子（煙突は煙でなくキラキラ）
- [x] 縦持ち・横持ち両対応（リサイズで自動レイアウト）
- [x] パレット：「姫の国 パレット表v1」40色のみ使用（`src/config/palette.js` に全色定義済み）
- [x] パフォーマンス：同時稼働アイテム上限300個（`CF.ITEM_CAP`）

### アセット差し替え構造（最重要要件への対応）

- 画像はすべて `src/config/assets.js` のマニフェスト経由。**`file: null` → プレースホルダー自動生成**、`file: 'assets/xxx.png'` を書けば差し替え完了。**コード変更は一切不要**
- 規約：基本タイル32px／設備は64px（2×2）／姫32×48px。**向きがあるものは「東向き（→）」で描く**（回転はコード側）

### 自動テスト

- `tools/smoke-test.js`：実ブラウザでライン一式を配置し、起動→生産→売上→セーブ復元→縦横スクショまで自動検証。**全項目PASS済み**

### 検品時に見てほしいところ（仮実装の調整ポイント）

- ベルト速度1.8マス/秒・泉の排出3秒間隔・研磨3秒の手触り
- ドラッグ敷設の気持ちよさ（スマホ実機での誤反応がないか）
- ピンチズームの効き具合・仮想スティックの位置（左下固定）

### ❓ 質問・未確定点（勝手に補完しなかったところ）

1. **設置コスト**：Phase 1は全設備タダで設置できる。指示書に費用の記載がなかったため。今リルは「貯まるだけ」なので、使い道（設置費 or Phase 2の拡張費）をどうするか決めてほしい
2. **研磨機の受け入れ条件**：「入力口の向きあり」を「機械の向きと同じ方向に流れて来たアイテムだけ入口面で受け入れる」と解釈した。横から差し込む合流も受け入れたい場合は1行で変えられる
3. **ベルトの曲がりの見た目**：ロジックは曲がり対応済みだが、プレースホルダー絵は直進絵の回転のみ（カーブ専用絵なし）。ドット素材を作る際にカーブ用の絵を用意するなら `assets.js` にキーを2〜3個足すだけで対応できる
4. **姫の移動**：仮想スティック方式を採用（タップ移動より工場のタップ操作と干渉しないため）。タップ移動の方が良ければ変更可
5. **納品箱の上限**：泉のみ上限2。納品箱・研磨機・ベルトは無制限でよい？

### 次（Phase 2着手はもえちゃん検品後）

- レシピツリー拡張（絹糸・リボン・レース・ドレス等）・販売システム本実装
